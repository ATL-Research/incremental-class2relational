-- Setup
library strings;

-- Helper
helper context String def: firstToLower() : String =
        if self.size() = 0
        then
                ''
        else
                self.substring(1, 1).toLower() + self.substring(2, self.size())
        endif;